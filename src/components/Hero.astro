---
// Hero component
---

<!-- Hero con fondo plano de marca y elementos decorativos simples -->
<section class="min-h-screen flex flex-col items-center justify-center text-center relative overflow-hidden section-padding-xl bg-[var(--brand-purple)]">
  <!-- Imagen de fondo del robot de IA (mantener, pero con más opacidad para contraste) -->
  <div class="absolute inset-0 bg-cover bg-center bg-no-repeat opacity-30" style="background-image: url('/top-learning-robot-bg.jpg');"></div>
  
  <!-- Elementos decorativos flotantes simplificados -->
  <div class="absolute top-20 left-10 w-24 h-24 bg-[var(--brand-orange)]/10 rounded-full blur-sm"></div>
  <div class="absolute top-40 right-20 w-20 h-20 bg-[var(--brand-purple)]/10 rounded-full blur-sm"></div>
  <div class="absolute bottom-40 left-20 w-16 h-16 bg-[var(--brand-gray)]/10 rounded-full blur-sm"></div>
  <div class="absolute top-1/2 right-10 w-12 h-12 bg-[var(--brand-orange)]/10 rounded-full blur-sm"></div>
  <div class="absolute bottom-20 right-1/4 w-8 h-8 bg-[var(--brand-purple)]/10 rounded-full blur-sm"></div>
  
  <div class="container-xl">
    <div class="relative z-10 animate-fade-in-up max-w-5xl mx-auto">
      <h2 class="text-4xl sm:text-5xl lg:text-7xl xl:text-8xl font-display mb-8 lg:mb-12 leading-tight text-[var(--brand-white)]">
        Potencia tu <span class="text-[var(--brand-orange)] font-display">negocio</span> con IA
      </h2>
      <p class="text-lg sm:text-xl lg:text-2xl xl:text-3xl text-[var(--brand-white)] max-w-4xl mx-auto mb-12 lg:mb-16 leading-relaxed font-body">
        Herramientas impulsadas por inteligencia artificial que transforman la forma en que automatizas tus procesos y conectas con tus clientes.
      </p>
      
      <!-- Botones de marca -->
      <div class="flex flex-col sm:flex-row gap-4 lg:gap-6 justify-center items-center mb-16">
        <a href="/signup" class="btn btn-primary btn-lg lg:btn-xl font-heading min-w-[200px] lg:min-w-[250px] group">
          <span class="relative z-10">Comenzar ahora</span>
        </a>
        <a href="#features" class="btn btn-secondary btn-lg lg:btn-xl font-heading min-w-[200px] lg:min-w-[250px] group">
          <span class="relative z-10">Ver más</span>
        </a>
      </div>
      
      <!-- Estadísticas rápidas -->
      <div class="grid grid-cols-3 gap-4 lg:gap-8 max-w-2xl mx-auto">
        <div class="text-center group">
          <div class="text-2xl lg:text-3xl font-display text-[var(--brand-orange)] mb-1">
            10K+
          </div>
          <p class="text-xs lg:text-sm text-[var(--brand-white)] font-heading">Clientes</p>
        </div>
        <div class="text-center group">
          <div class="text-2xl lg:text-3xl font-display text-[var(--brand-purple)] mb-1">
            99.9%
          </div>
          <p class="text-xs lg:text-sm text-[var(--brand-white)] font-heading">Uptime</p>
        </div>
        <div class="text-center group">
          <div class="text-2xl lg:text-3xl font-display text-[var(--brand-gray)] mb-1">
            50+
          </div>
          <p class="text-xs lg:text-sm text-[var(--brand-white)] font-heading">Apps</p>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Scroll indicator simplificado -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce-slow">
    <div class="w-6 h-10 border-2 border-[var(--brand-orange)] rounded-full flex justify-center cursor-pointer">
      <div class="w-1 h-3 bg-[var(--brand-orange)] rounded-full mt-2 animate-pulse"></div>
    </div>
  </div>
</section>
